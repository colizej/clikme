# SEO-стратегия — clikme.ru

## Текущие активы (март 2026)

- ~70 статей, позиции 1-10 по запросам о Вьетнаме/Нячанге
- Домен clikme.ru — возраст + ссылочная масса
- Googlebot активно посещает сайт (подтверждено логами)
- Семантическое ядро: рестораны, туризм, жильё, достопримечательности Нячанга

### Трафик по устройствам (Google Search Console, март 2026)
| Устройство | Клики | Показы | Доля |
|------------|--------|--------|------|
| 📱 Мобайльный | 8 879 | 296 647 | **82%** |
| 🖥️ ПК | 1 960 | 61 752 | 18% |
| 📱 Планшет | 58 | 2 164 | <1% |

> **Вывод: основная аудитория сидит в телефоне**. Весь дизайн и разработка ведутся принципа **mobile-first**.

---

## Правила которые нельзя нарушать при миграции

### 1. URL — неприкосновенны
```
✅ /vizaran-prodlenie-prebyvaniya-vo-vetname       ← оставить точно так
✅ /blog-soveti-history/romanticheskiy-uzhin-nyachang
❌ /blog/vizaran-prodlenie-prebyvaniya-vo-vetname  ← менять нельзя
❌ /articles/2024/vizaran...                       ← менять нельзя
```

Если URL всё же меняется — **обязательно 301-редирект** со старого на новый.

### 2. Title и Description — копировать дословно
Не "улучшать" при миграции — сначала перенести как есть, потом оптимизировать.

### 3. Canonical — на каждой странице
```html
<link rel="canonical" href="https://clikme.ru/{{ article.slug }}/">
```

### 4. sitemap.xml — обновлять и отправлять
- Генерировать через `django.contrib.sitemaps`
- Отправлять после каждой новой статьи в GSC и Яндекс.Вебмастер

---

## 📱 Mobile-First SEO

Google использует **mobile-first indexing** — сайт индексируется в первую очередь по мобильной версии.
С учётом 82% мобильного трафика это прямо влияет на позиции.

### Core Web Vitals — целевые метрики

| Метрика | Цель (Good) | Что делаем |
|---------|------------|-----------|
| **LCP** — загрузка главного контента | < 2.5s | WebP, `loading="eager"` у обложки, whitenoise |
| **CLS** — смещение контента | < 0.1 | `width`/`height` у всех `<img>`, нет динамических баннеров |
| **INP** — отклик на взаимодействие | < 200ms | Минимум JS, никаких тяжёлых библиотек |
| **TTFB** — время до первого байта | < 800ms | Django cache, sqlite WAL mode, gzip |

### Mobile UX чеклист

- [ ] `<meta name="viewport" content="width=device-width, initial-scale=1">` в каждом шаблоне
- [ ] Все кнопки и ссылки — tap target ≥ 44×44px (класс `min-h-[44px]`)
- [ ] `font-size` полей ввода ≥ 16px — иначе iOS Safari автозумирует страницу
- [ ] Таблицы в статьях: обёрнуты в `overflow-x-auto` для горизонтального скролла
- [ ] Навбар: hamburger на мобилке, полный только на `lg:`
- [ ] Карточки статей: 1 колонка на мобилке → `md:grid-cols-2` → `lg:grid-cols-3`
- [ ] Все изображения: `loading="lazy"` + явные `width`/`height` + WebP
- [ ] Hero-обложка статьи: `loading="eager"` (не lazy! — это LCP элемент)
- [ ] Нет горизонтального скролла страницы (`overflow-x-hidden` на body)
- [ ] Проверка в Google Search Console → "Удобство использования на мобильных"
- [ ] PageSpeed Insights ≥ 70 на мобилке перед каждым деплоем

---

## Технический SEO-чеклист Django

### Обязательно в Фазе 1

- [ ] `<title>` — уникальный для каждой страницы
- [ ] `<meta name="description">` — уникальный, 120-160 символов
- [ ] `<link rel="canonical">` — на каждой странице
- [ ] `robots.txt` — разрешить всё полезное, закрыть admin/
- [ ] `sitemap.xml` — все публичные статьи и категории
- [ ] Редиректы: www → non-www (или наоборот, консистентно)
- [ ] HTTPS — обязательно
- [ ] Скорость: сжатие gzip, кэш статики, оптимизация изображений
- [ ] Open Graph теги — для Telegram и ВКонтакте (важно для РФ аудитории)
- [ ] Hreflang — пока `ru`, при добавлении языков расширить

### robots.txt
```
User-agent: *
Allow: /
Disallow: /admin/
Disallow: /api/
Disallow: /accounts/

Sitemap: https://clikme.ru/sitemap.xml
```

### Структурированные данные (JSON-LD)

```python
# Для статьи блога
ARTICLE_SCHEMA = {
    "@context": "https://schema.org",
    "@type": "Article",
    "headline": "...",
    "description": "...",
    "image": "...",
    "datePublished": "...",
    "dateModified": "...",
    "author": {"@type": "Person", "name": "..."},
    "publisher": {
        "@type": "Organization",
        "name": "КликМи",
        "url": "https://clikme.ru"
    }
}

# Для локального бизнеса (каталог мест — Фаза 2)
PLACE_SCHEMA = {
    "@context": "https://schema.org",
    "@type": "Restaurant",  # или TouristAttraction, LodgingBusiness
    "name": "...",
    "address": {"@type": "PostalAddress", "addressLocality": "Нячанг"},
    "geo": {"@type": "GeoCoordinates", "latitude": "...", "longitude": "..."}
}
```

---

## Семантическое ядро для развития

### Высокочастотные запросы (уже есть позиции)
- Нячанг рестораны / кафе
- Виза во Вьетнам / визаран
- Достопримечательности Нячанга
- Перелёт во Вьетнам из России
- Пляжи Нячанга

### Средне- и низкочастотные (развивать)
- "Аренда жилья Нячанг" (экспаты)
- "Жизнь в Нячанге для русских"
- "Бюджет поездки во Вьетнам"
- "Медицина в Нячанге"
- "Работа в Нячанге для русских"
- Названия конкретных отелей, ресторанов, пляжей

### Коммерческие запросы (Trip.com монетизация)
- "Отели Нячанг забронировать"
- "Туры во Вьетнам 2026"
- "Экскурсии Нячанг"

---

## Мониторинг позиций

### Инструменты
- **Google Search Console** — бесплатно, основной
- **Яндекс.Вебмастер** — важен для РФ аудитории
- **Topvisor / Serpstat** — платно, для регулярного трекинга

### Что отслеживать
1. После миграции: ежедневно первые 2 недели
2. После переключения DNS: сравнение с baseline
3. Ключевые статьи с позиций 1-3: еженедельно

### Baseline (зафиксировать ДО миграции)
Скопировать из GSC топ-20 страниц с позициями в файл:
`docs/seo_baseline_[дата].csv`
